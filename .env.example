# URL of the Mahadev APF NestJS backend (server/).
# When set, the /api/leads route forwards site enquiries into the CRM pipeline.
# Leave unset to run the marketing site standalone (forms acknowledge without persisting).
BACKEND_API_URL="http://localhost:4000"
